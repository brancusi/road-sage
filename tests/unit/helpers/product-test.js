import { product } from 'roadsage/helpers/product';
import { module, test } from 'qunit';

module('Unit | Helper | product');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = product([42]);
  assert.ok(result);
});
